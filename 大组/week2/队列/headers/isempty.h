void isempty(LQueue*queue)
{
	 if (queue->length == 0)
	{  
        printf("队列是空的\n"); 
		system("pause"); 
        return;  
    }  
    else
    {
    printf("队列不是空的\n");
    system("pause");
    return;
	}
}
